// CyberBackground is no longer used in v1 enterprise design.
// Kept as an empty stub to avoid import errors if referenced anywhere.
import React from 'react';
export const CyberBackground: React.FC = () => null;
